# Example Spring Cloud application

The application was created for educational purposes.

# Installation
* install Docker
* change credentials in config files
* build services `docker-compose build`
* start services `docker-compose up`
